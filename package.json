{
  "name": "oasis-cache-sources",
  "private": true,
  "scripts": {
    "build": "esbuild --bundle --platform=node --target=node24 --outdir=dist save.js restore.js"
  },
  "dependencies": {
    "@actions/cache": "^6.0.0",
    "@actions/core": "^3.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
